# Data Classification

The v1.7 data-classification contract is frozen in [classification-redaction.md](classification-redaction.md).

This compatibility page exists because the original v1.7 kickoff named `docs/data-classification.md` as required release documentation. Keep both pages in sync if the classification model changes.
