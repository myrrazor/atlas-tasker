# Atlas Tasker v1.8.0-rc1 Release Evidence

Date: 2026-05-07

Version: `v1.8.0-rc1`

Branch stack: `codex/pr-801-v18-release-polish` through `codex/pr-809-v18-final-rc-evidence`

Local proof commit used by release scripts: `e6d76cbeeba3`

Decision: **no-ship from this session**.

The local RC proof is green. The public release is still blocked because hosted GitHub release proof did not pass and no `v1.8.0-rc1` release assets exist yet.

## Local RC Proof

| Gate | Result | Evidence |
| --- | --- | --- |
| whitespace check | passed | `git diff --check` |
| full tests | passed | `go test -count=1 ./... 2>&1 \| tee TEST_STDOUT.log` |
| vet | passed | `go vet ./...` |
| local preflight | passed | `docs/release-proof/preflight-v1.8.0-rc1.txt` |
| RC validator | passed | `docs/release-proof/validate-rc-v1.8.0-rc1.txt` |
| stability smoke | passed | `docs/release-proof/stability-smoke-v1.8.0-rc1.txt` |
| release rehearsal | passed | `docs/release-proof/release-rehearsal-v1.8.0-rc1.txt` |
| vulnerability scan | passed | `docs/release-proof/govulncheck-v1.8.0-rc1.txt` |
| SBOM | generated | `docs/release-proof/sbom-v1.8.0-rc1.cdx.json` |
| secret/local-path scan | passed | `scripts/validate-rc.sh` plus focused release-proof scan |

The final Go test output is committed in `TEST_STDOUT.log`.

## Security Proof

`govulncheck` ran through the pinned release preflight path:

```text
command=go run golang.org/x/vuln/cmd/govulncheck@v1.3.0 -show verbose ./...
go=go version go1.26.2 darwin/arm64
No vulnerabilities found.
```

CycloneDX SBOM generation completed with `cyclonedx-gomod@v1.10.0`:

- format: CycloneDX
- spec version: 1.6
- component count: 50
- file: `docs/release-proof/sbom-v1.8.0-rc1.cdx.json`

The release-proof logs committed with PR-809 were scanned for obvious private-key headers, token formats, and local absolute temp paths before commit.

## Packaged Rehearsal

The local rehearsal built `tracker_1.8.0-rc1_darwin_arm64.tar.gz`, generated checksums, verified the archive through `scripts/verify-release.sh` with attestations disabled for the local server, installed through `scripts/install.sh`, and ran the packaged smoke flow.

The smoke flow covered:

- workspace init
- project and ticket creation
- agent registration
- run dispatch, launch, start, checkpoint, evidence, and handoff
- gate approval, review request, ticket approval, and ticket completion
- export creation
- archive plan/apply/restore and run cleanup
- sync push/pull, conflict resolution, bundle create/verify/import
- compact, reindex, and doctor repair

The committed rehearsal proof is sanitized so local temporary paths are not preserved.

## Hosted Proof

Hosted proof did not pass.

Command:

```bash
VERSION=v1.8.0-rc1 sh scripts/preflight-release.sh --hosted
```

Result:

```text
preflight_status=blocked
reason_code=workflow_permissions_unreadable
message=GitHub Actions workflow permissions API is not readable with the current token
```

The release lookup also failed:

```bash
gh release view v1.8.0-rc1 -R myrrazor/atlas-tasker --json tagName,isPrerelease,isDraft,assets,url
```

Result:

```text
release not found
exit_code=1
```

Because no hosted prerelease assets exist from this session, these hosted gates were not run:

- hosted archive download
- published checksum verification
- GitHub artifact attestation verification
- install from published assets
- installed-binary version metadata comparison
- packaged smoke from a hosted binary

## CSO Closeout

GStack CSO / Codex security review was run for the v1.8 stack during PR-809. It returned `Clean` for the release-evidence scope; final gates were rerun afterward.

The CSO pass focused on:

- secret and private-key leakage in docs, scripts, examples, proof logs, and `TEST_STDOUT.log`
- dependency and SBOM proof
- GitHub release workflow permissions and attestation posture
- install and verify scripts
- MCP and agent-surface safety claims
- release wording that could overclaim ship readiness

Deferred items:

- Hosted release proof remains deferred to a release actor with the required GitHub permissions and published assets.
- Stable public release remains deferred until the hosted RC gate passes and the owner signs off.

## Final Decision

Local RC status: **green**.

Hosted RC status: **blocked**.

Ship decision: **no-ship from this session**.

Atlas Tasker `v1.8.0-rc1` can be reviewed as a local release candidate, but it should not be announced as a public release until the hosted gate in `docs/release/public-release-gates.md` passes.
