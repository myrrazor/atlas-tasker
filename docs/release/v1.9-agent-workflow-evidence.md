# v1.9 Agent Workflow Evidence

Status: local proof green.

## Implemented

- `tracker agent available [AGENT-ID]`
- `tracker agent pending [AGENT-ID]`
- MCP read tools `atlas.agent.available` and `atlas.agent.pending`
- TUI Queues tab backed by the shared agent-work service
- Installable Codex, Claude, and generic Atlas worker skill/command templates
- Local wake-up records for newly unblocked assigned agent work
- Owner-enabled command auto mode with argv-only execution and shell-interpreter rejection

## Deterministic Gates

The final PR refreshes `TEST_STDOUT.log` with:

```bash
go test -count=1 ./...
```

Result: passed on 2026-05-10.

Additional gates:

```bash
go vet ./...
git diff --check
sh scripts/stability-smoke.sh
```

Results:

- `go vet ./...`: passed
- `git diff --check`: passed
- `sh scripts/stability-smoke.sh`: passed
- targeted secret scan: no new live secrets found; matches were existing synthetic test fixtures

## Release Notes

This is not a hosted-agent release. Atlas remains local-first. Wake-ups create records and notifications by default; command mode is explicit local process launch configured by `human:owner`.
