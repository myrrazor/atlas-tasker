# v1.9 Agent Workflow Evidence

Status: local proof green.

## Implemented

- `tracker agent available [AGENT-ID]`
- `tracker agent pending [AGENT-ID]`
- MCP read tools `atlas.agent.available` and `atlas.agent.pending`
- TUI Queues tab backed by the shared agent-work service
- Installable Codex, Claude, and generic Atlas worker skill/command templates
- Local wake-up records for newly unblocked assigned agent work
- Owner-enabled command auto mode with argv-only execution and shell-interpreter rejection
- autonomy-first ticket approval: agents may self-approve by default when no required reviewer or governance separation policy applies
- `ticket_approve` remains an optional governance protected action for explicit separation-of-duties
- strict `blocked_by` workflow enforcement with owner-only `--override-deps` and audit payloads
- MCP `atlas.ticket.request_review` reviewer parity with CLI `--reviewer`
- direct bundle verify reports missing sidecars with `sidecar_manifest_missing`

## Deterministic Gates

The final PR refreshes `TEST_STDOUT.log` with:

```bash
go test -count=1 ./...
```

Result: passed on 2026-05-10.

Additional gates:

```bash
go vet ./...
git diff --check
sh scripts/stability-smoke.sh
```

Results:

- `go vet ./...`: passed
- `git diff --check`: passed
- `sh scripts/stability-smoke.sh`: passed
- targeted secret scan: no new live secrets found; matches were existing synthetic test fixtures

## Focused Sandbox Proof

A fresh binary built from PR-908 was exercised in a temp workspace on 2026-05-10:

```bash
go build -o /tmp/atlas-pr908/tracker ./cmd/tracker
```

Sandbox workspace: temporary `/tmp/atlas-pr908-sandbox.*` directory, not committed.

Verified:

- project `required_reviewer=agent:reviewer-1` plus `review_gate` rejects builder approval and accepts reviewer approval
- autonomous assignee/reviewer self-approval and completion succeed without governance SoD
- governance `ticket_approve` separation-of-duties blocks same-actor approval when enabled
- unresolved `blocked_by` rejects unsafe progress
- `human:owner --override-deps --reason <TEXT>` progresses a blocked ticket and records `dependency_override`
- after the blocker reaches `done`, Board Blocked moves to `Blocked (0)`
- reindex preserves the same board state
- slash shell request-review with reviewer and bulk dry-run wording work
- MCP reviewer parity smoke passed with `go test ./internal/mcp -run TestTicketRequestReviewToolAcceptsReviewer -count=1`

## Release Notes

This is not a hosted-agent release. Atlas remains local-first. Wake-ups create records and notifications by default; command mode is explicit local process launch configured by `human:owner`.
