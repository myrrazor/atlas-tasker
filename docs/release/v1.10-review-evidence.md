# v1.10 release review — 2026-09-08

The remaining six PRs were reviewed together against `testing` at
`5c42491de5b37efb814a79eda739cdf41097eb00`, then rebuilt as a sequence of
individually tested updates. The sequence is #117, #121, #122, #126, #127, #123.
All target `testing`; each later branch includes the earlier branches. Merge in
that order. The owner retains merge, promotion, tag, and publication control.

## Review and security posture

The GStack review and scoped CSO code/diff check covered the release changes,
their callers, storage transactions, authorization, secret output, local file
boundaries, CLI/JSON/MCP contracts, and CI/release configuration. Specialist
reviews covered testing, maintainability, security, API contracts, and projection
integrity. Two review/fix rounds completed. Source inspection and executed tests
are separate evidence below.

Verified findings were corrected:

| Finding and concrete failure path | Correction and regression evidence |
| --- | --- |
| High, confidence 10/10: `config set --json` serialized `"value": args[1]`, exposing webhook credentials in transcripts. | Reuse the config getter's masking function; `TestConfigSetJSONDoesNotEchoWebhookSecrets` also verifies the stored value remains intact. |
| High, confidence 10/10: an empty `--host` reached `net.Listen` before the server normalized it, binding wildcard interfaces. | Normalize and reject hosts before listening; verify the actual listener is loopback. Empty, localhost, IPv4/IPv6 wildcard, and direct-server listener cases are covered. |
| High, confidence 10/10: integration output paths passed lexical containment checks while symlinks redirected writes outside the repo. | Preflight all destination components before the first write. File, directory, instruction-file, and integration-directory symlink tests preserve external fixtures. |
| High, confidence 9/10: corrupt-index reset removed the database before obtaining the workspace lock. | Doctor repair and reindex acquire the canonical write lock before reset/open/rebuild. Busy recovery preserves existing index bytes. |
| High, confidence 10/10: full reindex replaced the database inode while existing web/MCP/TUI pools retained the previous file. Failed project rebuilds could also erase rows. | Transactional rebuilds in the existing database preserve live readers and roll back failures. Live-reader, rollback, concurrent-first-open, and pooled busy-timeout tests pass. |
| Medium, confidence 10/10: an incremental projection stamp could hide an earlier skipped event after a later apply succeeded. | Advance only a contiguous source-count watermark; complete replay stamps all sources. Skipped-event and duplicate-import tests pass. |
| Medium, confidence 10/10: MCP/TUI and config reads bypassed the wrong-directory guard and could create state or return misleading defaults. | Shared initialized-root validation with CLI, MCP, and TUI regression tests; explicit init and integration bootstrap remain supported. |
| Medium, confidence 10/10: reviewer relevance exposed promote/claim/start recommendations for another assignee's backlog ticket. | Restrict promotion recommendations to the assignee, unassigned claimable work, or owner; denied policy, lease, disabled-agent, and capability cases also preserve state. |

Existing session/CSRF/origin/read-only web controls, escaped rendering,
parameterized projection queries, owner-only executable automation, and export
symlink checks were traced and exercised by the relevant suites. No remaining
verified release blocker was found in the inspected scope. This is not an audit
of unrelated local branches, the host machine, third-party MCP clients, or a
published production deployment.

The independent Claude challenge was attempted, but its configured model
returned HTTP 429 and required usage credits before reviewing any input. No
Claude review is claimed. A nested Codex invocation was skipped because this
review already ran under Codex. The completed specialist reviews remain the
independent source checks for this run.

## Executed local validation

Go 1.26.6 was used with x/net v0.56.0, x/term v0.44.0, x/sys v0.46.0,
x/text v0.39.0, and goldmark v1.7.17. Heavy checks ran sequentially.

| Check | Result |
| --- | --- |
| `go test -p 2 -count=1 ./...` and `go vet -p 2 ./...` | Passed on the combined candidate; each rebuilt intermediate PR was also tested. Full suite output is retained in `TEST_STDOUT.log`. |
| `sh scripts/stability-smoke.sh` | Passed race checks for service/CLI/TUI and all five fuzz targets. |
| `VERSION=v1.10.0-rc1 sh scripts/validate-rc.sh` | Passed docs/snippets, quickstart, terminal accessibility, JSON parity, performance budgets, and real MCP stdio initialize/tools-call from another directory. |
| `VERSION=v1.10.0-rc1 sh scripts/release-rehearsal.sh` | Passed archive checksum, isolated install, version metadata, agent/review workflow, export/archive, and multi-workspace synchronization. |
| Local preflight with `RUN_GOVULNCHECK=1 RUN_SBOM=1` | Passed. Pinned govulncheck v1.3.0 reported no vulnerabilities; CycloneDX v1.10.0 generated the SBOM from the tested binary. |
| Full release-history gitleaks v8.28.0, `git . --log-opts=HEAD --redact --no-banner` | Passed with no leaks. No rules or individual findings were suppressed. |
| `sh scripts/check-workflow-security.sh` | Passed for every YAML workflow; action SHAs, least privilege, and Go version policy are checked. |
| Browser refresh and website contract tests | All 26 passed. Both are included in CI. |
| Four supported release archive builds | Linux and macOS, each amd64/arm64, built and passed archive/architecture inspection. The native darwin/arm64 executable passed version metadata checks. Cross-compiled binaries were not executed locally. |
| Local browser QA | Created a ticket through the board, closed its drawer, refreshed an already-running server after a separate CLI reindex, checked desktop/mobile layouts, and loaded Schedule. No console errors were observed. |
| Hosted settings preflight | Passed: public repository, readable Actions settings, read-only workflow defaults, and required release workflow grants. |

The initial gitleaks invocation inspected every local branch and flagged a
`fileToken` in an unmerged Pen design commit
`4282c81fbf1b2bb1a55fc45207681ad4cf16129b`. That commit is absent from the release
ancestry and every remote branch. It was neither deleted nor allowlisted. The
release result above covers the entire candidate ancestry, not just its diff.
No conclusion about that unrelated design token's validity is claimed here.

## Decision audit and remaining gates

DEC-051 records shared workspace, masking, installer, and listener boundaries.
DEC-052 supersedes the file-swap and watermark portions of DEC-050 and V13-005.
DEC-053 records the dependency and validation contract; DEC-054 records promotion
eligibility; DEC-055 records binary-based local SBOM generation. Historical
decisions remain present with supersession links and rationale.

Source-count freshness still does not detect same-count manual content edits.
Stop live consumers before deleting or replacing a corrupt database. Local
filesystem checks reject existing symlinks; they do not isolate Atlas from a
malicious local process racing filesystem operations.

The local release gate passes. Inspect the current checks on all six updated
PR heads before merging. A stable public release remains **no-ship** until the
owner merges/promotes the release, creates an RC tag, verifies the published
archives and attestations through the hosted install smoke, completes human
README/docs/aesthetic review, and records sign-off. No merge, tag, deployment,
or publication was performed by this review.
