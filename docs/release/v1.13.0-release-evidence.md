# v1.13.0 Source Approval and Release Evidence

Status at this source snapshot: **owner-approved for promotion and publication;
hosted proof pending**. The owner merged [PR #141](https://github.com/myrrazor/atlas-tasker/pull/141)
into `testing` and authorized the remaining PR merges, required account reviews,
and the full release on 2026-09-10 UTC. The five PR checks passed on
`fc0758bad1c66de23f1a2d317d68abc09de0b002`; the merged tree at
`8b3ebf5aec2f87553dd1c8b8fe7096c1351c733e` is byte-identical.

Post-publication results are recorded on the immutable-tag
[v1.13.0 release page](https://github.com/myrrazor/atlas-tasker/releases/tag/v1.13.0),
which becomes the authority for hosted verification after publication. Pending
items below describe this source snapshot, not a later release-page verdict.

## Scope and compatibility

[The 22-finding assessment](workflow-consistency-review-2026-09-09.md) records the
agreed fixes, retained safeguards, and the live-index issue that did not reproduce.
The candidate includes:

- Separate Canceled board columns and visible assignees across the affected views.
- Consistent CLI identity resolution, explicit reassignment before a different
  worker can claim, and rejection of new schedules at or before the current time.
- Effective review presets, inherited project policy, a configured reviewer, and
  permission checks before review approval can complete work.
- Six additional MCP workflow tools and explicit startup workspace bootstrap.
- Portable generated guidance and current documentation, website copy, and board
  captures.

Version 1.13 is a minor release because it adds public MCP tools and a startup
option. Consumers must treat `columns.canceled` separately from `columns.done`.
Scripts that relied on an implicit owner must pass `--actor`, set `TRACKER_ACTOR`,
or configure `actor.default`. Applying a review preset clears project `open`
overrides so they inherit the selected workspace policy; explicit non-open project
overrides and custom agent/profile/runbook definitions remain unchanged. See
[team presets](../guides/team-presets.md) before applying a preset to an existing
workspace.

No new runtime dependency or storage-schema version is introduced. Read-only MCP
defaults, strict schemas, separate client registration, external approval issuance,
and the existing ready-before-work workflow remain deliberate boundaries.

## Verification

Full output is preserved in [TEST_STDOUT.log](../../TEST_STDOUT.log). Candidate
verification includes the complete Go suite and vet, CLI/service/MCP authorization
regressions, installer and real stdio MCP harnesses, race and fuzz lanes, dependency
and candidate secret checks, and actual browser captures. The preparation pass adds
local v1.13.0-rc1 packaging and SBOM, offline RC validation, and clean-directory
installation rehearsal. All passed on macOS arm64 with Go 1.26.6. The 40 web/site
contracts also passed. Local rehearsal metadata identifies pre-publication commit
`2ca2ee62886b`; published artifacts must identify the final approved release commit.

The final bounded review found no runtime defect. It identified one missing
authorization regression: approving a review-gated ticket while `gate_approve` is
allowed and `ticket_complete` is denied. The new regression passes, proves the
ticket and event log are unchanged on denial, and verifies both allowed completion
and open-mode approval. This closes the third and final review round for this scope.
DEC-061 through DEC-069 record the decisions and release boundaries.

The documentation audit covers current README/agent guidance, guides, tutorials,
references, and MCP inventories. The website audit covers home, CLI, onboarding,
workflow, board, FAQ, MCP, and changelog pages. Historical versioned material remains
historical. Final website version labels and download references are prepared for
v1.13.0; deploy that payload only after the stable hosted checks pass.

A real browser checked 14 affected public pages at 390 and 1440 pixels: all 28
page/viewport checks had one main heading and no horizontal page overflow. The
five homepage image instances decoded successfully, including the refreshed board
asset. Desktop and phone web captures were inspected after layout settled. The
CLI board image renders actual `COLUMNS=160 tracker board` output, including
assignee and Canceled columns; it is not a simulated application screen.
[Structured docs/site proof](v1.13.0-docs-site-proof.json) records these checks.

## Remaining release gates

1. Final release-metadata changes pass local and hosted checks and merge into
   `testing` under the owner's release authorization.
2. The approved source completes the `testing` to `main` promotion, including the
   active code-owner review requirement and exact promoted-head checks.
3. Publish `v1.13.0-rc1` from that approved commit; verify all four archives, checksums,
   SBOM, attestations, clean install, metadata, packaged workflow, and MCP/installer
   harnesses against the downloaded native executable.
4. Publish `v1.13.0` after hosted RC proof and stable sign-off, then repeat stable
   artifact and unpinned-latest installation verification.
5. Deploy and verify the matching public site, including source-byte comparison,
   links, responsive rendering, and agent onboarding. Record hosted evidence on
   the release pages without moving existing tags.

Local proof does not satisfy hosted artifact or production deployment gates.
No hosted result is implied by this source snapshot. Existing tags remain immutable.
