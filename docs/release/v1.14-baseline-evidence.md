# v1.14 Baseline Freeze Evidence (AT114-001)

This page records the frozen starting point of the v1.14 release train and the baseline test
results, then the Sprint 114.0 results on the feature branch. Procedural release SHA/checksum
ceremony is waived by owner override; the commit and tree identifiers below exist so reviewers can
reproduce the runs, not as a release gate.

## Baseline

| Item | Value |
| --- | --- |
| Base commit | `b95fd640c5b6bd956aadf5ac08d4b5b460e33de2` ("Merge pull request #143 from myrrazor/testing", 2026-09-09) |
| Base tree | `21d08ea3c2f40840b1730325a515b2d8b533cf64` |
| Equal to | `origin/dev`, `origin/main`, `origin/testing`, tags `v1.13.0` and `v1.13.0-rc1` (all resolve to the base commit) |
| Feature branch | `feat/v1.14-seamless-agent-backup`, created from `origin/dev` at the base commit |
| Worktree | separate `git worktree` at `<HOME>/atlas-v114`; the pre-existing checkout at `/workspace` (branch `cursor/setup-cloud-agent-environment-dfd9`), its environment config, demo workspace, and running processes were not modified |
| Toolchain | go1.26.6 linux/amd64, Python 3.12.3, node v22.14.0 |

### Local work beyond public main (audited, not mixed)

| Ref | Ahead of base | Content | Disposition |
| --- | --- | --- | --- |
| `cursor/setup-cloud-agent-environment-dfd9` (`80d57e9`) | 1 commit | adds `.cursor/environment.json` (4 lines) for the Cloud Agent environment | unrelated to v1.14; left untouched |
| `backup/setup-env-original-56fc50d` (`56fc50d`) | local backup ref of the environment-setup work | unrelated; left untouched |
| `origin/cursor/v1-11-stable-ship-4138` (`4e59e79`) | older remote branch from a previous session | explicitly not resurrected |

### Clean state

`git status --short --untracked-files=all` in the new worktree listed nothing before the first
v1.14 file was created. Through the Sprint 114.0 freeze commits no pre-existing file other than
`docs/v1-decision-log.md` (which gained DEC-070..DEC-075) was modified (`git diff --stat
--diff-filter=M b95fd640..<freeze head>` lists only that file); every other Sprint 114.0 change is a
new file. The review round 1 fix commits modify only files that Sprint 114.0 itself introduced,
plus the decision log. The first implementation commit has the base commit as its direct parent.

## Baseline gate run (pristine detached worktree at the base commit)

Run with a throwaway `HOME`/`XDG_*` so no real provider configuration is touched, real
`GOCACHE`/`GOMODCACHE` retained, load checked before and after (1.58 → 2.75 one-minute average on
a 4-core VM). Log: `TEST_STDOUT.baseline.log` in that temporary worktree (not committed; the
sprint runs below are committed as `TEST_STDOUT.log`), 313 lines, 21:18:49Z–21:22:02Z on
2026-09-10.

| Step | Command | Result |
| --- | --- | --- |
| 1 | `go build -o ./tracker ./cmd/tracker` | exit 0 |
| 2 | `go vet ./...` | exit 0 |
| 3 | `sh -n scripts/install.sh scripts/verify-release.sh` | exit 0 |
| 4 | `git diff --check` | exit 0 |
| 5 | `go test ./...` | exit 0, 19 `ok` packages + 2 without test files, 0 FAIL |
| 6 | `python3 scripts/test-install.py --tracker ./tracker` | exit 0, 6 tests OK |
| 7 | `python3 scripts/verify-mcp-workflow.py --tracker ./tracker` | exit 0, `"status": "passed"`; profile counts read 41 / workflow 73 / delivery 77 (79 with flag) / admin 77 (88 with flag); bootstrap, safety, both transports, and the full workflow sequence passed |
| 8 | `node --test internal/web/testdata/refresh_board.test.mjs site/_tools/*.test.mjs` | exit 0, 40 pass / 0 fail |
| 9 | `sh scripts/stability-smoke.sh` | exit 0, race runs `ok` for service/cli/tui, five fuzz targets PASS |

This matches the CI gates in `.github/workflows/ci.yml`.

## Sprint 114.0 gate run (feature branch)

### Sprint 114.0 results (after review round 1 fixes)

Run on the feature branch at commit `122fa912bcb06d9c3341a95cc56549fb5e33f08a` (tree
`6a27e58945e6c6eb7554a2827a5b7b5014a20c37`), the head that contains the four review round 1 fix
commits, with `GOFLAGS=-count=1`, a throwaway `HOME`/`XDG_*`, real Go caches, and no
`TRACKER_ACTOR` in the environment. Load 0.03 → 2.40 one-minute average on the 4-core VM,
23:29:56Z–23:32:44Z on 2026-09-10. The complete sanitized output (314 lines; the fake home and the
real home directory are replaced by `<FAKE_HOME>`/`<HOME>`) is committed as `TEST_STDOUT.log` at
the repository root in the commit that follows the fixes.

| Step | Command | Result |
| --- | --- | --- |
| 1 | `go build -o ./tracker ./cmd/tracker` | exit 0 |
| 2 | `go vet ./...` | exit 0 |
| 3 | `sh -n scripts/install.sh scripts/verify-release.sh` | exit 0 |
| 4 | `git diff --check` | exit 0 (clean tree; the four fix commits were also checked with `git diff --check` before committing) |
| 5 | `go test ./...` | exit 0, 21 `ok` packages + 2 without test files, 0 FAIL; the two new packages `internal/integrations/adapter` and `internal/setup` are included |
| 6 | `python3 scripts/test-install.py --tracker ./tracker` | exit 0, 6 tests OK |
| 7 | `python3 scripts/verify-mcp-workflow.py --tracker ./tracker` | exit 0, `"status": "passed"`; profile counts unchanged at read 41 / workflow 73 / delivery 77 (79 with flag) / admin 77 (88 with flag); no MCP surface changed in Sprint 114.0 |
| 8 | `node --test internal/web/testdata/refresh_board.test.mjs site/_tools/*.test.mjs` | exit 0, 40 pass / 0 fail |
| 9 | `sh scripts/stability-smoke.sh` | exit 0, race runs `ok` for service/cli/tui, five fuzz targets PASS |

Focused evidence for the Sprint 114.0 packages, run separately with race detection and coverage
(not part of the gate script): `internal/integrations/adapter` 55 tests, 94.2%; `internal/setup`
7 tests, 100.0%; `internal/contracts` managed-mode functions 100% except two unreachable
marshal-error branches. The reviewer's overlay reproducers re-run against this head are summarized
in [v1.14-s0-review-round1.md](v1.14-s0-review-round1.md) §6.

Not run in this sprint, by design: no real agent client (none is installed on the VM), no live
provider configuration, no backup of real data. Sprint 114.0 contains contracts and documents only;
the gate proves that the existing product is unchanged and the new packages are green.
