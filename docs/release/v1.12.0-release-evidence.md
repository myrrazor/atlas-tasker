# v1.12.0 Release Evidence

Status: **local candidate checks passed; awaiting PR review and hosted CI; not published**. The current published stable release is v1.11.0. The owner requested v1.12 implementation and validation, followed by a concrete PR approval request before publication.

## Scope

- Update the README, source docs, public Docs pages, and Connect Your Agent journey for the current MCP workflow, self-updater, and all six integration targets.
- Offer optional coding-agent setup after a verified interactive shell installation, and preserve unattended installation and explicit cancellation.
- Accept all six shipped web languages as saved workspace preferences.
- Replace public web captures with the current UI and synthetic `User` identity; reuse the current UI wordmark in README, website, and social preview.
- Preserve published Git history and tags. Historical authorship and old file versions remain reachable; current-tree privacy cleanup does not claim a history scrub.

The candidate is based on the published v1.11 source and its stable-evidence follow-up in PR #136. The feature branch starts from dev and includes those already published changes because dev/testing had remained on v1.10. It will enter the normal testing-to-main promotion flow.

## Verification recorded during preparation

| Check | Result |
| --- | --- |
| Required Go and workflow checks | Fresh `go test -count=1 ./...`, `go vet ./...`, and workflow supply-chain policy passed with Go 1.26.6. |
| Browser and site contracts | All 36 Node tests passed, including the web-board refresh contract, site links/metadata, tool/profile copy, and canonical wordmark parity. |
| Installer PTY and unattended rehearsal | Passed six scenarios: checksum rejection, non-interactive install, default decline, explicit opt-out, selected-target setup, and explicit `none`. Uses local synthetic archives; this is not hosted provenance proof. |
| MCP stdio workflow | Passed both NDJSON and Content-Length framing from an unrelated client directory with explicit `--workspace`. Exact inventories: read 41, workflow 67, delivery/admin without high-impact 71, delivery with high-impact 73, admin with high-impact 82. |
| MCP workflow and authorization | Read-profile writes denied. Synthetic team/agent/ticket creation, assignment, dependency linking, queue/goal visibility, separated reviewer/owner approvals, completion, automatic dependent promotion, wake-up and acknowledgement passed. No high-impact or provider operation executed. |
| Website browser checks | Passed home, Docs, Connect Your Agent, MCP setup and MCP tools at 320, 390, 768 and 1440 CSS pixels without horizontal page overflow or broken loaded images. Six tabs, keyboard wrap, mobile Docs menu and visible keyboard focus passed; no console errors. |
| Copy-button behavior | The actual browser click handlers delivered exact snippets to an isolated clipboard test boundary, showed manual-copy guidance when denied, and cleaned up the legacy fallback textarea. No user clipboard or agent configuration was changed. |
| Self-update against hosted v1.11.0 | Check and dry-run preserved the disposable executable. An explicit forced install of the current stable release passed checksum and GitHub attestation verification, replaced only the task-owned binary, reported the published commit, and passed a same-version update check. |
| Public images and wordmark | Current UI captured from synthetic projects at desktop/phone sizes. Both public wordmark copies are byte-identical to the UI asset; social preview is reproducible from checked-in HTML. Offline OCR inspected 20 raster assets across 488 frames with no personal-name/path matches; changed image containers carry no textual metadata. |
| Demo fixtures | Regenerated CLI outputs match the candidate; the fixture check passed. The synthetic web fixture rejected non-empty and symlink destinations without changing either. |
| Local release packaging | v1.12.0-rc1 preflight, packaged RC validation, and clean-directory installation/workflow rehearsal passed. Version metadata, documentation links, snippet checks, and the packaged workflow passed. These are local builds, not published RC artifacts. |
| Stability | The existing race lane for service/CLI/TUI and all five existing fuzz smoke targets passed. |
| Dependency and secret checks | `govulncheck` passed and CycloneDX SBOM generation succeeded. Gitleaks found no leaks in the complete candidate ancestry or an export of all 651 tracked/unignored candidate files. |

Fresh required suite output is recorded in [TEST_STDOUT.log](../../TEST_STDOUT.log). It retains initial failures and their successful reruns, with local paths and trailing whitespace normalized. The stale generated fixtures and one short page-description assertion were corrected. A broader scan of all local branches flagged UUID-shaped `fileToken` metadata in an unmerged design branch outside the candidate ancestry. Scanning the raw working directory also found ephemeral tokens in ignored browser-session files; these are absent from the candidate export and are not PR content. No scanner allowlist or history rewrite was added.

Hosted CI and hosted RC/stable results remain separate from this local candidate proof. Local release builds made during preparation carried the pre-commit base identifier; published artifacts must be built and verified against the final approved commit.

## Review and decision audit

The `/review` pass covered the changed Go/shell/Python/CI behavior and current documentation. Two findings were fixed in the first review round: delimiter-only picker input now fails without scaffolding a workspace, and the MCP harness now fails on nonzero or forced server shutdown while preserving an earlier exception. Regression checks and the complete MCP workflow passed after those changes. The focused CSO source review reported no verified security findings; it supplements the execution evidence above.

| Decision | Implementation evidence |
| --- | --- |
| DEC-057: explicit setup consent and cancellation | Verified-binary-before-prompt ordering, terminal/no-terminal installer scenarios, buffered-answer preservation, EOF/cancel handling, and invalid-selection no-write tests. |
| DEC-058: six languages and neutral presentation | Saved-config and actual-catalog render tests for all six languages; current synthetic UI screenshots; source/asset identity checks. DEC-036 links to its superseding decision. |
| DEC-059: candidate proof and owner promotion | Fresh required suite, real MCP/installer CI additions, generated examples, and explicit unfulfilled hosted gates. DEC-002 links to the superseding release-branch policy. |

The reviewed behavior matches these decisions. The candidate has no new runtime dependency, storage-schema change, provider permission change, or published tag.

## Required publication gates

1. Required candidate checks and decision audit pass; owner reviews the final PR head and presentation.
2. Owner-approved promotion reaches main without rewriting published tags or unrelated work.
3. Publish v1.12.0-rc1 and verify the hosted archives, checksums, attestations, installer, installed metadata, SBOM and packaged workflow.
4. Publish v1.12.0 after hosted RC proof passes, then repeat stable artifact and unpinned-latest installer verification.
5. Record post-publication verification on the immutable release pages and refresh the public current-release references.

**Decision: no publication until owner approval and hosted proof are recorded.**
