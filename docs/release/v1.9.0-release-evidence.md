# v1.9.0 Release Evidence

First stable public release. This file records the hosted proof for `v1.9.0-rc2` and the owner ship decision that promotes it to `v1.9.0`, per `docs/release/public-release-gates.md`.

## Hosted RC proof: v1.9.0-rc2 (2026-06-10)

Tagged from `1ab31fb` (main after PRs #109, #110, #111, #112). The release workflow published all assets with build attestations:

- `tracker_1.9.0-rc2_{darwin,linux}_{amd64,arm64}.tar.gz` (4 archives)
- `checksums.txt`, `sbom-v1.9.0-rc2.cdx.json`, `install.sh`
- marked prerelease

| Gate step | Result |
|---|---|
| Installer from published assets (`curl ... install.sh \| VERSION=v1.9.0-rc2 sh`) into a clean directory | `installed tracker v1.9.0-rc2` — checksum and GitHub build attestation verified by the installer |
| Version metadata match | `tracker version --json` reports `version=v1.9.0-rc2`, `commit=1ab31fb520385e87f8fabc2384b983ede31e949e`, `platform=darwin/arm64`, `go1.26.4` |
| `scripts/verify-release.sh` on downloaded `darwin_arm64` archive | `verified tracker_1.9.0-rc2_darwin_arm64.tar.gz for v1.9.0-rc2` |
| Packaged smoke (init, project, ticket, board) | pass |
| CI on the tagged commit | all four lanes green (test x2, stability, security incl. govulncheck on go1.26.4, full-history gitleaks, SBOM) |
| Offline RC validation (`scripts/validate_rc.py`) | all checks ok: version contract, docs links, snippets, stale versions, leakage, README quickstart, terminal accessibility, cross-surface parity, performance budgets |

Local RC proof for the same train (tests, vet, race lanes, validate_rc) is green and recorded in the PR bodies for #109/#110/#111.

## Stable gate

- Hosted RC gate: **green** (above).
- MIT `LICENSE`: committed.
- Human README/docs review: performed by the owner across the v1.9 polish wave (README rewrite review, brand/wordmark placement, splash legibility revision requested and verified).
- Owner sign-off: the owner directed the stable release on 2026-06-10 ("let's get a stable release out"). Recorded here as the ship decision.

**Decision: SHIP `v1.9.0`** — tag the commit that contains this evidence file; the release workflow publishes the stable (non-prerelease) assets, which makes the unpinned README install command resolve via `releases/latest`.

## Post-tag verification (to complete after `v1.9.0` publishes)

- Bare installer one-liner (no `VERSION` pin) installs `v1.9.0` from `releases/latest` with verification: **pending tag**
- `tracker version --json` matches the stable tag: **pending tag**
- Packaged smoke from the installed stable binary: **pending tag**

## Branch hygiene

Five superseded long-lived branches were deleted with documented supersession receipts: see `branch-cleanup-receipts.md`.
