# v1.14 release-candidate evidence (AT114-605)

This is an implementation candidate on the stacked Sprint 114.6 branch. It is not a published release, tag, or off-device backup claim.

Procedural source/binary SHA/checksum/attestation ceremony is waived as a completion gate (owner override). Product backup integrity and installer checksum verification are not waived.

## What this candidate contains

Sprints 114.1–114.6 on top of the approved Sprint 114.0 contracts:

- Unified, repairable `tracker setup`
- Six provider adapters and the existing atlas-worker skill
- Managed mode, `atlas.context`, `atlas.status`, board Markdown/MCP App
- Automatic local checkpoints and off-device publication (synthetic `file://` only in this environment)
- Cohesive setup first-checkpoint verify, upgrade/repair fixtures, security review tests

## Honest remaining dependencies

| Item | Why it remains |
| --- | --- |
| Real Codex / Claude Code / Cursor / OpenClaw / Grok sessions | Named-host NL and live MCP doctor/probe require those clients |
| Real user `systemctl --user enable` / LaunchAgent load | Fixture-home units are tested; host enablement is operator-run |
| macOS arm64 / amd64 installer matrix | This candidate is proven on Linux amd64 |
| 10k tickets / 100k events / 10k checkpoint commits | Smaller reuse/soak exists; full soak is still scheduled |
| Owner-authorized private SSH/HTTPS disaster drill | Execution overrides forbid a real remote in this run |
| Release publication | Separate explicit owner decision |

## Residual non-blocking P1s (recorded, not blocking this candidate)

- Crash after local `update-ref` and before ledger persist can create a redundant local commit (same tree).
- `protocol.file.allow=always` is pinned for disposable remotes (DEC-088).
- `git init --bare` symbolic HEAD may be `refs/heads/atlas-backup` with no commit on that branch. Replica refs remain `refs/atlas/backups/...`.
- Unchanged-tree skip does not reset `PendingEventCount`.
- Full soak and real-client rows above.

`backup target edit` now requires `--allow-local-file` for `file://` URLs, matching add. Leading or trailing whitespace on the URL is trimmed before the `file://` consent check, so a padded `file://` path cannot bypass the flag.

## P2 dispositions

Sprint 114.0 P2s remain closed at contract scope or assigned as implemented later-sprint work (see `docs/release/v1.14-s0-review-round1.md` and `docs/v1.14-acceptance.md`). Exported-only collector roots stay unrestored after the AT114-507 drill; the restore-safe allowlist was not widened.

## Release remains blocked (plan list) — current honesty

| Blocker | Status on this candidate |
| --- | --- |
| False connected for a named provider | Deterministic adapters do not claim verified from files alone |
| Adapter overwrites unrelated config | Covered by adapter tests |
| Generic claimed universally connected | Portable-ready only |
| High-impact MCP enabled by setup | Setup uses workflow profile defaults |
| Mutation waits for remote backup | Mutations succeed when backup is offline |
| Backup touches user HEAD/index | Git-isolation tests |
| Origin silently selected | Target add never infers origin |
| Remote verification missing | First setup checkpoint + publish/verify path |
| Restore bypasses planner | Remote restore uses restore-plan/apply + G-A |
| Backup recursion | Automatic checkpoints append no canonical events |
| Source/secrets in checkpoint | Allowlist + drill |
| Real disaster recovery | Remaining dependency (synthetic drill exists) |
| Independent review unresolved P0/P1 | Requires the Sprint 114.6 independent review |
