# Audit Reports

The v1.7 audit report and audit packet contract is frozen in [audit-model.md](audit-model.md).

This compatibility page exists because the original v1.7 kickoff named `docs/audit-reports.md` as required release documentation. Keep both pages in sync if the audit model changes.
