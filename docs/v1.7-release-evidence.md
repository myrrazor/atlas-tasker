# Atlas Tasker v1.7 Release Evidence

Date: 2026-05-07

PR-708 records the release-candidate proof for the v1.7 trust/governance/security train. Local proof is green. Hosted prerelease proof is not complete from this session, so v1.7 is **not ship-ready** until a release actor completes the hosted gate below.

## Local Proof

Passed:

- `sh scripts/stability-smoke.sh`
- `VERSION=v1.7.0-rc1 ./scripts/release-rehearsal.sh`
- `git diff --check && go vet ./... && go test -count=1 ./... 2>&1 | tee TEST_STDOUT.log`

Packaged rehearsal result:

- version: `v1.7.0-rc1`
- archive: `tracker_1.7.0-rc1_darwin_arm64.tar.gz`
- local checksum verification: passed
- local install smoke: passed
- single-workspace run/evidence/handoff/gate/ticket-complete flow: passed
- multi-workspace sync/archive rehearsal: passed

The final full Go test output is captured in `TEST_STDOUT.log`.

## Security And Hardening Proof

PR-708 adds seed fuzz coverage for:

- restore-plan path validation
- goal-manifest JSON decode/validation

PR-708 adds adversarial regression coverage for:

- goal manifest signatures fail verification after `source_hash` tampering

The final PR-708 gate also re-runs prior PR-707 regression coverage for:

- backup restore rejects corrupted archive contents before any restore write
- goal brief generation fails closed when related context cannot load
- direct corrupt ticket goal targets return the ticket load error instead of falling through to run lookup

Review proof:

- `codex review --uncommitted` found restore integrity, goal context loading, and goal-manifest contract issues in PR-707.
- Those findings were fixed before PR-707 was opened.
- A scoped `codex exec` re-review checked the remaining direct-ticket fallthrough path; the PR-707 base includes `TestGoalBriefFailsClosedOnCorruptTicketTarget`.
- `codex review --uncommitted` on PR-708 found two evidence wording issues and one PR attribution issue; all three were corrected before this PR was opened.

## Hosted Proof

Blocked:

- command: `sh scripts/preflight-release-proof.sh`
- result: `preflight_status=blocked`
- reason code: `workflow_permissions_unreadable`
- GitHub API status: `403`

This session can verify the release workflow file and local rehearsal, but it cannot verify repository Actions workflow-permission settings through the current token. Do not call v1.7 release-ready until a release actor with the needed GitHub permission completes hosted prerelease proof.

Required hosted gate:

1. Run `sh scripts/preflight-release-proof.sh` with a token that can read repository Actions workflow permissions.
2. Create the selected prerelease tag, such as `v1.7.0-rc1` or a later candidate tag if more fixes land.
3. Wait for the GitHub release workflow to publish archives and `checksums.txt`.
4. Download at least one published archive.
5. Run `VERSION=<selected-candidate-version> ./scripts/verify-release.sh <archive-path>` with attestation verification enabled.
6. Record the archive name, checksum result, and `gh attestation verify` result here before promoting anything to a stable release.

## Vulnerability And SBOM

`govulncheck ./...` was attempted but the binary is not installed in this environment:

```text
govulncheck unavailable
```

SBOM tooling was checked with `syft`, `cyclonedx-gomod`, and `bom`; none were installed. Per D-1707, SBOM generation is deferred for this local PR-708 proof because no low-risk local SBOM tool is available in the environment.

Before a stable v1.7 release, record both of these outputs:

- vulnerability scan: `govulncheck ./...`
- SBOM generation: `syft . -o cyclonedx-json`, `cyclonedx-gomod app -json -output sbom.cdx.json`, or `bom`

## Release Decision

Decision: **no-ship from this session**.

Reason: local RC proof passed, but hosted prerelease proof and SBOM/vulnerability tooling evidence are not complete. This is a release-governance block, not a known product-code failure.
