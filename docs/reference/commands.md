# Command Reference

The full command inventory lives in [../command-reference.md](../command-reference.md).

For quick discovery, the CLI is the source of truth:

```bash
tracker --help
tracker ticket --help
tracker run --help
tracker goal --help
tracker mcp --help
tracker version --json
```

PR-803 retitles the historical command page to a v1.8 release-candidate reference. PR-808 adds automated command/documentation parity checks.
