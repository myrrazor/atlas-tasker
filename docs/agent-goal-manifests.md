# Agent Goal Manifests

The v1.7 agent goal manifest contract is frozen in [goal-manifests.md](goal-manifests.md).

This compatibility page exists because the original v1.7 kickoff named `docs/agent-goal-manifests.md` as required release documentation. Keep both pages in sync if the goal output model changes.
