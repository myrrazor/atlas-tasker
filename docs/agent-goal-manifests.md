# Agent Goal Manifests

The agent goal manifest contract is frozen in [goal-manifests.md](goal-manifests.md).

This compatibility page exists because the original security train named `docs/agent-goal-manifests.md` as required release documentation. Keep both pages in sync if the goal output model changes.
