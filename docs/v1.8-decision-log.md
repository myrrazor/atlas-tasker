# Atlas Tasker v1.8 Decision Log

This log records v1.8 release-candidate polish decisions. v1.8 does not redesign Atlas. It makes the product public-readable, cleaner to operate, easier for coding agents to use, and better proven as a release candidate.

## D-1801 — v1.8 Starts From The v1.7.1 Closeout Tip

- Date: 2026-05-07
- Question: Where should v1.8 start while the v1.7 stack is still open?
- Options Considered:
  - Wait until PRs #68-#76 merge into `main`, then start PR-801 from updated `main`.
  - Start from current `main`, treating v1.7/v1.7.1 as missing blockers.
  - Stack PR-801 on `codex/pr-709-v171-release-signoff` at `5a75aec336311abd1844c8578342e7da0af5388e`, then retarget or rebase after the v1.7 stack merges.
- Chosen Option: Stack PR-801 on the v1.7.1 closeout tip.
- Why We Chose It: The v1.8 kickoff assumes v1.7/v1.7.1 and the MCP RC are part of the product surface. Starting from current `main` would make the docs and release proof lie about available commands and security posture.
- Confidence: high
- Revisit Trigger: PRs #68-#76 merge to `main`, or GitHub reports a stack conflict that cannot be solved by a straight rebase.
- Affected PRs/Files: PR-801, `docs/v1.8-base.json`, all v1.8 branches

## D-1802 — v1.8 Is Release Polish, Not A New Subsystem Train

- Date: 2026-05-07
- Question: What kind of work belongs in v1.8?
- Options Considered:
  - Add another product subsystem before public launch.
  - Limit v1.8 to public release polish, docs, terminal/TUI polish, agent UX, demos, release validation, and security review.
- Chosen Option: v1.8 is release-candidate polish only.
- Why We Chose It: Atlas already has the core local orchestration, governance, signing, redaction, audit, backup, goal, and MCP surfaces. The remaining first-release gap is trust from humans: readable docs, crisp examples, clean terminal output, and reproducible release proof.
- Confidence: high
- Revisit Trigger: A release-blocking bug requires a narrow product fix.
- Affected PRs/Files: PR-801 through PR-809

## D-1803 — Public Version Is `v1.8.0-rc1`

- Date: 2026-05-07
- Question: What release version should v1.8 prepare?
- Options Considered:
  - `v0.1.0`, treating this as the first public OSS release.
  - `v1.0.0-rc1`, treating this as the first v1 public candidate.
  - `v1.8.0-rc1`, keeping the existing train version as the public candidate tag.
- Chosen Option: Use `v1.8.0-rc1`.
- Why We Chose It: Existing release scripts, evidence style, and versioned docs already follow the train version. Renaming the public version now would create avoidable translation work during the release closeout.
- Confidence: high
- Revisit Trigger: The repository owner chooses a separate public semver reset before tagging a hosted prerelease.
- Affected PRs/Files: PR-807, PR-809, release docs and scripts

## D-1804 — Public Claims Carry The v1.7 No-Ship Block Forward

- Date: 2026-05-07
- Question: Can v1.8 docs claim the product is ready to ship before hosted proof exists?
- Options Considered:
  - Let README and release docs describe v1.8 as ready after local tests.
  - Clearly mark v1.8 as release-candidate work until hosted assets, checksums, attestations, install smoke, SBOM, vulnerability scan, and final evidence pass.
- Chosen Option: Keep a no-ship release gate until hosted proof passes.
- Why We Chose It: v1.7.1 local proof is green, but hosted prerelease assets do not exist from this session and the current token cannot verify GitHub Actions workflow permissions. Public docs can be polished without overstating release status.
- Confidence: high
- Revisit Trigger: A release actor records successful hosted prerelease proof in `docs/release/v1.8-release-evidence.md`.
- Affected PRs/Files: PR-801, PR-807, PR-809, `docs/release/public-release-gates.md`

## D-1805 — Security Wording Stays Precise And Limited

- Date: 2026-05-07
- Question: How should public docs describe Atlas security?
- Options Considered:
  - Use broad launch language such as secure, enterprise-ready, encrypted, or identity-backed.
  - State exact claims: trusted local-key signature verification, app-level governance, structured redaction for Atlas-owned data, signed audit packets, and side-effect-safe restore planning.
- Chosen Option: Use exact security claims and repeat limitations wherever release trust is discussed.
- Why We Chose It: Atlas is local-first software, not an OS sandbox, hosted identity provider, encrypted vault, or DLP system. Overclaiming security would be worse than saying the true boundary plainly.
- Confidence: high
- Revisit Trigger: Atlas adds encrypted storage, hosted identity, or OS sandbox support.
- Affected PRs/Files: PR-802, PR-803, PR-807, PR-809, `docs/security-limitations.md`

## D-1806 — Docs Architecture Separates Public Docs From Historical Plans

- Date: 2026-05-07
- Question: How should the docs tree be organized for GitHub readers?
- Options Considered:
  - Keep linking historical implementation plans as the main docs entrypoint.
  - Add a public docs landing page and group docs into getting started, tutorials, concepts, guides, reference, troubleshooting, release, and historical planning.
- Chosen Option: Add a public docs architecture and keep historical plans discoverable but secondary.
- Why We Chose It: New users need the first ten minutes, command examples, agent workflows, and release verification first. Historical PR plans remain valuable for maintainers, but they should not be the first docs path.
- Confidence: high
- Revisit Trigger: The docs site moves from GitHub Markdown to a generated website.
- Affected PRs/Files: PR-802, PR-803, `docs/docs-architecture.md`

## D-1807 — OSS Hygiene Is In Scope

- Date: 2026-05-07
- Question: Are license, support, governance, security policy, roadmap, and GitHub templates part of v1.8?
- Options Considered:
  - Defer OSS hygiene until after the release candidate.
  - Include the minimum credible GitHub project metadata in PR-802.
- Chosen Option: Include OSS hygiene in PR-802.
- Why We Chose It: Public polish without `LICENSE`, `SECURITY.md`, templates, and contribution guidance makes the project look unfinished and creates avoidable friction for early adopters.
- Confidence: high
- Revisit Trigger: The owner wants a non-MIT license or a private disclosure process that differs from the default policy.
- Affected PRs/Files: PR-802, `.github/`, root metadata files

## D-1808 — Review Tooling Is Required But Not A Hard Blocker When Unavailable

- Date: 2026-05-07
- Question: How should the v1.8 train treat GStack/Codex review tooling failures?
- Options Considered:
  - Block each PR indefinitely until every model review runs.
  - Attempt the requested pre/post reviews, record failures, and keep deterministic local gates mandatory.
- Chosen Option: Attempt GStack/Codex reviews and record unavailable tooling; deterministic tests remain the release gate.
- Why We Chose It: Model reviews are useful, but release proof must be reproducible. Tooling availability should not stop a docs or polish PR when tests and manual review can still prove the change.
- Confidence: high
- Revisit Trigger: A required security review finds a high-confidence blocking issue.
- Affected PRs/Files: PR-801 through PR-809, `docs/v1.8-decision-log.md`

## PR-801 Pre-Implementation Review

- Tooling: GStack-style pre-review via `codex exec` in read-only mode.
- Result: accepted.
- Accepted Findings:
  - create the missing v1.8 docs
  - carry forward the hosted-proof no-ship block
  - freeze product positioning
  - freeze public security wording
  - make release gates a single source of truth
  - separate public docs from historical plans
  - include OSS hygiene in v1.8 scope

## PR-801 Post-Implementation Review

- Tooling: `codex review --uncommitted` following the GStack review workflow.
- Result: one actionable docs finding fixed.
- Finding: `docs/release/public-release-gates.md` referenced `scripts/preflight-release.sh` before PR-807 creates that wrapper.
- Fix: the hosted gate now references the currently executable `scripts/preflight-release-proof.sh` and notes that PR-807 adds the friendlier wrapper.
- Verification rerun: clean; no discrete correctness issues found in the staged docs.

## D-1809 — License Selection Is Deferred

- Date: 2026-05-07
- Question: Should PR-802 add a license file?
- Options Considered:
  - Add MIT or Apache-2.0 as a reasonable open-source default.
  - Defer the license until the repository owner selects public reuse terms.
- Chosen Option: Defer license selection and state that public reuse terms are not finalized.
- Why We Chose It: A license changes legal reuse rights. The pre-implementation review correctly flagged that this needs owner confirmation rather than an agent assumption.
- Confidence: high
- Revisit Trigger: The owner selects a license.
- Affected PRs/Files: PR-802, `README.md`, `GOVERNANCE.md`, future `LICENSE`

## PR-802 Pre-Implementation Review

- Tooling: GStack-style pre-review via `codex exec` in read-only mode.
- Result: accepted.
- Accepted Findings:
  - do not claim a stable public release
  - mirror `docs/security-limitations.md` exactly in public security language
  - do not invent a license or copyright holder
  - do not make `curl | sh` the only trusted install path
  - avoid links to PR-803 docs that do not exist yet
  - keep public contributor flow separate from internal Merlin/orchestrator branch rules
  - provide a realistic vulnerability reporting path without pretending a private channel is configured
  - warn issue reporters not to include secrets, private keys, `.tracker` payloads, or unredacted logs

## PR-802 Post-Implementation Review

- Tooling: `codex review --uncommitted` following the GStack review workflow.
- Result: clean.
- Notes:
  - no discrete correctness, security, or workflow-breaking issues found in the staged README, metadata, and GitHub template diff
  - the reviewer attempted an extra source-build quickstart probe, but that probe could not download the Go toolchain because DNS resolution for `proxy.golang.org` failed in the review sandbox
- Deterministic Gates: local `git diff --check`, `go test ./...`, `go vet ./...`, Markdown link checks, and GitHub issue-template YAML parsing passed before commit.

## PR-803 Pre-Implementation Review

- Tooling: GStack-style docs review via `codex exec` in read-only mode.
- Result: accepted.
- Accepted Findings:
  - create the missing public docs route map promised by `docs/docs-architecture.md`
  - replace stale v1.6 release wording with current v1.8 RC release-gate language
  - retitle or route the command reference as a v1.8 release-candidate reference
  - update README docs links to the user-first docs system
  - expand doctor/repair guidance to separate read-only checks from mutating projection repair
  - label installer and release verification snippets with hosted-asset prerequisites and failure modes

## PR-803 Post-Implementation Review

- Tooling: `codex review --uncommitted` following the GStack review workflow.
- Result: one actionable docs finding fixed.
- Finding: the first agent tutorial could fail at dispatch with `dirty_repo` when readers followed the source-build path and had local build/projection files in the git working tree.
- Fix: the agent workflow docs now require a clean git workspace before dispatch and show `.git/info/exclude` entries for `tracker`, `.tracker/write.lock`, `.tracker/index.sqlite`, `.tracker/index.sqlite-wal`, and `.tracker/index.sqlite-shm` before committing tutorial state.
- Verification rerun: the documented clean-repo dispatch setup was smoke-tested in a temp git workspace and returned a run ID.
- Follow-up Findings:
  - tutorials 3-5 incorrectly switched from the source-built `./tracker` path to `tracker`
  - the file-layout reference documented `.tracker/db/` instead of the actual `.tracker/index.sqlite` projection path
- Follow-up Fix: tutorials 3-5 now keep `./tracker`, the first-agent workflow uses the same source-built binary path, SQLite WAL/SHM sidecars are excluded before dispatch, gate approval examples use the required `agent:reviewer-1` actor, and the file-layout reference names `.tracker/index.sqlite`.
- Final Review: `codex review --uncommitted` reported no blocking docs correctness issues after the fixes.

## PR-804 Pre-Implementation Review

- Tooling: GStack-style implementation review via `codex exec`.
- Result: accepted.
- Accepted Findings:
  - unify TUI and shared renderer color/plain detection so `NO_COLOR` and non-TTY behavior stay consistent with DEC-010
  - make width tests use injectable widths instead of probing the caller's terminal
  - make truncation display-width aware and apply it before styling
  - keep badges semantic and ASCII-stable instead of icon-only
  - keep render tokens in the presentation layer only
  - keep TUI width tests focused on body/list/footer behavior
  - improve empty states without making them verbose

## PR-804 Post-Implementation Review

- Tooling: `codex review --uncommitted` following the GStack review workflow.
- Result: clean.
- Notes:
  - no discrete actionable regressions were found in the shared renderer, TUI, or CLI expectation updates
  - the review sandbox hit an unrelated `httptest` listener failure while probing the full CLI package; the workspace deterministic gates below were rerun outside that sandbox
- Deterministic Gates: `git diff --check`, targeted render/TUI/CLI tests, `go test ./...`, and `go vet ./...`.

## PR-805 Pre-Implementation Review

- Tooling: GStack-style agent workflow review via `codex exec`.
- Result: accepted.
- Accepted Findings:
  - freeze the v1.8 goal section contract in code, not just docs
  - keep `tracker goal brief` and `tracker goal manifest` on the same Markdown/JSON section order
  - avoid duplicate goal headings in Markdown output
  - add public docs under `docs/guides/` for Codex, Codex `/goal`, Claude Code, generic agents, and MCP agent use
  - audit MCP docs against live tool inventory and current v1.7 governance behavior
  - add golden goal Markdown/JSON tests, run-target coverage, manifest persistence/signature coverage, and MCP docs inventory parity

## D-1810 — v1.8 Goal Sections Replace The v1.7 Goal Headings

- Date: 2026-05-07
- Question: Should v1.8 keep the v1.7 12-section goal headings or adopt the release-candidate agent prompt headings?
- Options Considered:
  - Keep the v1.7 headings to avoid changing persisted manifest shape.
  - Adopt the v1.8 headings and update contract validation, builders, Markdown, tests, and docs together.
- Chosen Option: Adopt the v1.8 headings.
- Why We Chose It: PR-805 is the release-candidate agent workflow polish PR, and the public interface explicitly freezes the agent-ready section names. Keeping the old names would make Codex `/goal`, docs, JSON, and manifest signatures diverge.
- Confidence: high
- Revisit Trigger: A future manifest format version adds backward-compatible rendering for old signed manifests.
- Affected PRs/Files: PR-805, `internal/contracts/goal.go`, `internal/service/backup_goal_actions.go`, `internal/cli/security_commands.go`, goal/MCP docs and tests

## PR-805 Post-Implementation Review

- Tooling: `codex review --uncommitted` following the GStack review workflow.
- Result: two actionable agent-workflow findings fixed, final review clean.
- Findings And Fixes:
  - `Current State` mixed a real status line with the `None` blocker fallback; the builder now filters fallback-only blockers before composing state and the service test asserts the regression.
  - `Suggested Commands` emitted `tracker goal brief <target>` without `--md`; generated commands now use the paste-ready Markdown form and service tests cover ticket and run targets.
  - run-target suggested evidence commands used invalid evidence type `test`; generated commands, generated integration guides, and public agent guides now use `test_result`, with service and installer tests covering the value.
- Notes:
  - final review found no discrete regressions after the fixes
  - the review sandbox still cannot run unrelated `httptest` listener tests, so local full-suite results remain the authoritative gate
- Deterministic Gates: `git diff --check`, targeted service/CLI/integration tests, `go test ./...`, `go vet ./...`, and Markdown link checks passed locally.

## PR-806 Pre-Implementation Review

- Tooling: GStack-style demo-assets review via `codex exec` in read-only mode.
- Result: accepted.
- Accepted Findings:
  - executable regeneration scripts should live under top-level `examples/`, while checked-in prompt packs, transcripts, and screenshot-friendly outputs should live under `docs/examples/`
  - no new `tracker demo` command is needed for the RC
  - demo generation must use a temp git workspace, commit tutorial state before dispatch, capture generated run/evidence/handoff/goal IDs, and normalize volatile IDs, timestamps, and local paths
  - PR-806 needs an offline freshness gate that regenerates assets and fails on stale files, absolute local paths, or obvious secrets
  - the implementation plan branch name should match the actual branch: `codex/pr-806-v18-demo-material`

## D-1811 — Demo Assets Are Generated, Not Product Commands

- Date: 2026-05-07
- Question: Should v1.8 add a `tracker demo` product command or keep demos as release assets?
- Options Considered:
  - Add a `tracker demo` command that seeds a workspace.
  - Add top-level example scripts that generate checked-in docs examples.
- Chosen Option: Use `examples/generate-demo-assets.sh` and checked-in `docs/examples/` outputs.
- Why We Chose It: PR-806 is release polish, not a product-workflow expansion. A script can build a temporary git workspace, prove the tutorial flow, and refresh public examples without adding a CLI surface that would need long-term compatibility guarantees.
- Confidence: high
- Revisit Trigger: Users need built-in demo seeding after the public release and the workflow has stabilized.
- Affected PRs/Files: PR-806, `examples/`, `docs/examples/`, `docs/v1.8-implementation-plan.md`

## PR-806 Post-Implementation Review

- Tooling: `codex review --uncommitted` following the GStack review workflow.
- Result: one actionable script finding fixed, final review clean.
- Finding: the generated-output leak gate skipped silently when `rg` was unavailable.
- Fix: `examples/generate-demo-assets.sh` now requires `rg` or falls back to `grep -R -n -E`; the fallback path was smoke-tested with `rg` removed from `PATH`.
- Deterministic Gates: `git diff --check`, `sh -n examples/generate-demo-assets.sh`, `sh examples/generate-demo-assets.sh --check`, fallback scanner smoke, docs link check, generated-output leakage scan, `go test ./...`, and `go vet ./...`.

## PR-807 Pre-Implementation Review

- Tooling: GStack-style release-distribution review via `codex exec` in read-only mode.
- Result: accepted.
- Accepted Findings:
  - freeze the exact `tracker version --json` fields and text output contract in docs and tests
  - inject release metadata through Go build-time variables and `-ldflags`, with sane defaults for `go run` and source builds
  - keep `scripts/preflight-release.sh` POSIX `sh` compatible and preserve `scripts/preflight-release-proof.sh` output keys and failure behavior
  - pin SBOM and `govulncheck` commands, define proof output locations, and separate PR-807 local proof from final PR-809 ship evidence
  - document hosted verification as a concrete command sequence: download hosted assets, verify checksums and attestations, install from the archive, run packaged smoke, and compare `tracker version --json` to the tag/commit/platform
  - keep PR-807 scoped to `tracker version` plus release proof tooling only

## D-1812 — Release Preflight Separates Local Proof From Hosted Proof

- Date: 2026-05-07
- Question: Should the PR-807 public preflight wrapper fail by default when hosted GitHub release proof is unavailable?
- Options Considered:
  - Always require hosted GitHub proof from `scripts/preflight-release.sh`.
  - Make local preflight the default and require an explicit hosted mode for GitHub proof.
- Chosen Option: Default `scripts/preflight-release.sh` to local proof checks and make hosted proof explicit.
- Why We Chose It: v1.8 branches can prove scripts, version metadata, SBOM generation, vulnerability scanning, and local release rehearsal before hosted assets exist. Hosted checksum and attestation verification still block final release sign-off and PR-809 evidence.
- Confidence: high
- Revisit Trigger: GitHub release assets for `v1.8.0-rc1` exist and the release token can read required repository Actions permissions.
- Affected PRs/Files: PR-807, `scripts/preflight-release.sh`, `scripts/preflight-release-proof.sh`, release docs and evidence docs

## PR-807 Post-Implementation Review

- Tooling: `codex review --uncommitted` following the GStack review workflow.
- Result: one actionable release-workflow finding fixed.
- Finding: `.github/workflows/release.yml` executed `dist/tracker version --json` for every matrix build, which would fail for cross-compiled targets such as `linux/arm64` on an `amd64` Ubuntu runner.
- Fix: the release workflow now detects the runner `GOOS/GOARCH` and runs the version smoke only for native targets while still stamping metadata into every archive through `-ldflags`.
- Review Sandbox Note: the review sandbox's own `go test ./...` probe hit existing `httptest` listener permission failures; local deterministic gates outside the sandbox passed and remain authoritative.
- Final Review: rerun `codex review --uncommitted` reported no discrete correctness, security, or release-blocking issues.
- Deterministic Gates: `git diff --check`, shell syntax checks, `tracker version` focused tests, `go test ./...`, `go vet ./...`, docs link check, no-secret scan, `VERSION=v1.8.0-rc1 sh scripts/preflight-release.sh`, `RUN_GOVULNCHECK=1 RUN_SBOM=1` preflight with a temp proof dir, `sh scripts/stability-smoke.sh`, and `VERSION=v1.8.0-rc1 ./scripts/release-rehearsal.sh`.

## PR-808 Pre-Implementation Review

- Tooling: GStack-style RC-validation plan review via `codex exec` in read-only mode.
- Result: accepted.
- Accepted Findings:
  - replace the one-line PR-808 scope with explicit validation entrypoints and check ownership
  - define exact pass/fail rules for terminal widths, `NO_COLOR` behavior, non-TTY-style output, performance budgets, command parity, docs scope, and release-text scope
  - add allowlists so stale-version and leakage scans fail current public regressions without tripping on historical version docs or intentional sanitizer/test-pattern lines
  - make README quickstart smoke offline, temp-workspace based, and independent of hosted release assets
  - keep PR-808 limited to repo/release validation scripts and docs; no new product commands, storage contracts, or user workflow behavior
  - record the validation strategy as a decision-log entry

## D-1813 — RC Validation Is An Offline Release Script Gate

- Date: 2026-05-07
- Question: How should v1.8 prove release polish regressions without adding product workflow surface?
- Options Considered:
  - Add new product commands such as `tracker validate` or `tracker release check`.
  - Add one offline repo script that builds a temp binary, seeds temp workspaces, and validates public docs, snippets, terminal output, parity, leakage, versions, and performance budgets.
- Chosen Option: Add `scripts/validate-rc.sh` backed by `scripts/validate_rc.py`.
- Why We Chose It: PR-808 is release-polish proof, not a product API change. A repo script can run in CI or locally, avoid network and hosted assets, encode precise release-candidate gates, and keep allowlists reviewable without creating a long-term CLI compatibility promise.
- Confidence: high
- Revisit Trigger: The validation checks become useful to end users outside the release process, or hosted release proof needs a single operator-facing command.
- Affected PRs/Files: PR-808, `scripts/validate-rc.sh`, `scripts/validate_rc.py`, release docs, `docs/v1.8-pr-breakdown.md`

## PR-808 Post-Implementation Review

- Tooling: GStack-style post-review via `codex exec` and `codex review --uncommitted`.
- Result: accepted findings fixed; final targeted verification reported clean.
- Accepted Findings:
  - protect `VALIDATE_RC_WORK_DIR` from deleting caller-provided directories by creating and cleaning a child temp directory
  - keep leakage allowlists narrow and scan Python scripts, not only docs/shell files
  - match badge-escaped stale RC strings such as `v1.7.1--rc1`
  - avoid overclaiming MCP parity; the validator verifies CLI/slash-shell JSON parity and required MCP read-profile tool presence
  - enforce actual `COLUMNS=40/80` width ceilings instead of loose 100/120-character caps
  - expand actor-required snippet detection across Atlas write surfaces and add self-tests for representative commands
  - require exact `--actor`/`--reason` flag tokens so `--actor-map` cannot satisfy actor checks
  - split chained shell snippets so a later command cannot satisfy an earlier command's actor requirement
  - use display-width accounting for wide Unicode runes in terminal checks
  - include timeline output in width enforcement and truncate board/dashboard/timeline pretty output under configured widths
- Deterministic Gates: `git diff --check`, `sh -n scripts/validate-rc.sh`, `python3 -m py_compile scripts/validate_rc.py`, `VERSION=v1.8.0-rc1 sh scripts/validate-rc.sh`, `go test ./internal/render ./internal/cli`, `go test ./...`, and `go vet ./...`.

## PR-809 Pre-Implementation Review

- Tooling: GStack-style PR-809 plan review via `codex exec` in read-only mode.
- Result: accepted.
- Accepted Findings:
  - keep PR-809 evidence-only with no product feature or CLI surface changes
  - add final release evidence with local proof, hosted proof transcript, CSO closeout, exact blocked gates, and final ship/no-ship decision
  - add a launch checklist that separates local RC green, hosted RC green, and stable owner sign-off
  - update changelog/release notes without claiming stable release until hosted gates pass
  - regenerate `TEST_STDOUT.log` from `go test -count=1 ./...`
  - record PR-809 closeout because ship/no-ship and security review affect release scope

## D-1814 — v1.8.0-rc1 Is Local-Green But No-Ship Until Hosted Proof Exists

- Date: 2026-05-07
- Question: Can PR-809 sign off `v1.8.0-rc1` as a public release after local proof passes?
- Options Considered:
  - Treat the local RC proof, SBOM, vulnerability scan, and release rehearsal as enough to ship publicly.
  - Record local RC proof as green but keep release sign-off blocked until hosted assets, checksums, attestations, install smoke, packaged smoke, and owner sign-off pass.
- Chosen Option: Record local RC proof as green and keep the final decision as no-ship from this session.
- Why We Chose It: The release boundary is hosted provenance, not local packaging. The current token cannot read required GitHub Actions workflow permission settings, and no `v1.8.0-rc1` GitHub release exists yet.
- Confidence: high
- Revisit Trigger: A release actor with the required GitHub permissions creates the prerelease and records hosted checksum, attestation, install, version metadata, and packaged smoke proof.
- Affected PRs/Files: PR-809, `docs/release/v1.8-release-evidence.md`, `docs/release/launch-checklist.md`, `docs/release/v1.8.0-rc1-release-notes.md`, `CHANGELOG.md`, `TEST_STDOUT.log`, `docs/release-proof/`

## PR-809 Post-Implementation Review

- Tooling: GStack CSO-style security closeout via `codex exec` in read-only mode.
- Result: clean.
- CSO Scope:
  - docs, scripts, proof logs, and `TEST_STDOUT.log` secret/private-key leakage
  - local absolute path leakage in committed release proof
  - release overclaiming and no-ship wording
  - hosted proof gaps, GitHub attestation/install risk, and MCP/client safety claims
- Hosted Proof Result: blocked at `VERSION=v1.8.0-rc1 sh scripts/preflight-release.sh --hosted` with `reason_code=workflow_permissions_unreadable`; `gh release view v1.8.0-rc1` returned `release not found`.
- Local Proof Result: local RC gates passed; `govulncheck` reported no vulnerabilities and CycloneDX SBOM generation completed.
- Deterministic Gates: `go test -count=1 ./... 2>&1 | tee TEST_STDOUT.log`, `go vet ./...`, `git diff --check`, `VERSION=v1.8.0-rc1 sh scripts/preflight-release.sh`, `VERSION=v1.8.0-rc1 sh scripts/validate-rc.sh`, `sh scripts/stability-smoke.sh`, `VERSION=v1.8.0-rc1 ./scripts/release-rehearsal.sh`, `VERSION=v1.8.0-rc1 RUN_GOVULNCHECK=1 RUN_SBOM=1 RELEASE_PROOF_DIR=docs/release-proof sh scripts/preflight-release.sh`, hosted preflight attempt, and focused proof-log leakage scan.

## D-1815 — Public Release Requires License And Human Taste Sign-Off

- Date: 2026-05-07
- Question: Can v1.8 be called a public release candidate after local proof if license selection and human first-impression review are still pending?
- Options Considered:
  - Treat local RC proof plus generated docs as enough for public release-candidate status.
  - Keep public ship blocked until hosted proof, owner-selected license, and human README/docs/aesthetic review are complete.
- Chosen Option: Keep public ship blocked until all three are complete.
- Why We Chose It: Hosted provenance proves the artifact path, the license grants actual reuse rights, and a human taste pass is required for the public first impression. Codex can validate docs mechanically, but it should not choose legal terms or claim aesthetic sign-off.
- Confidence: high
- Revisit Trigger: The owner commits a license, verifies hosted assets, and records human review sign-off.
- Affected PRs/Files: PR-810, `README.md`, `docs/release/v1.8.1-release-owner-closeout.md`, `docs/release/launch-checklist.md`, `docs/release/public-release-gates.md`, `docs/release.md`

## PR-810 Post-Implementation Review

- Tooling: Codex read-only review of the v1.8.1 release-owner closeout diff.
- Result: one release-wording finding fixed.
- Finding: README release sign-off wording named hosted gates as the only blocker, while D-1815 also requires a committed owner-selected `LICENSE` and human README/docs/aesthetic review.
- Fix: README now names all three blockers before release sign-off.
- Deterministic Gates: `git diff --check`, `VERSION=v1.8.0-rc1 sh scripts/validate-rc.sh`, `go test ./...`, and `go vet ./...`.
