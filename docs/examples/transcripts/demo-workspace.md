# Demo Workspace Transcript

This transcript is regenerated by `examples/generate-demo-assets.sh`. Volatile timestamps, IDs, and local paths are normalized.

```console
$ tracker init
initialized

$ tracker project create APP "Example App"
created project APP

$ tracker ticket create --project APP --title "Add health check" --type task --actor human:owner --reason "demo setup" --acceptance ...
created APP-1

$ tracker ticket move APP-1 ready --actor human:owner --reason "ready for implementation"
moved APP-1 to ready

$ tracker ticket claim APP-1 --actor agent:builder-1
claimed APP-1 as work until <TIMESTAMP>

$ tracker ticket move APP-1 in_progress --actor agent:builder-1 --reason "start implementation"
moved APP-1 to in_progress

$ tracker agent create builder-1 --name "Builder One" --provider codex --capability go --actor human:owner --reason "register demo agent"
saved agent builder-1

$ tracker run dispatch APP-1 --agent builder-1 --actor human:owner --reason "start demo run" --json
{
  "format_version": "v1",
  "generated_at": "<TIMESTAMP>",
  "kind": "run_dispatch_result",
  "payload": {
    "agent_id": "builder-1",
    "generated_at": "<TIMESTAMP>",
    "run_id": "<RUN-ID>",
    "runbook": "implement",
    "stage": "implement",
    "ticket_id": "APP-1",
    "worktree_path": "<TMP_PATH>"
  }
}

$ tracker run start <RUN-ID> --summary "Demo implementation started" --actor agent:builder-1 --reason "begin work"
started <RUN-ID>

$ tracker run checkpoint <RUN-ID> --title "First pass" --body "Health check route added locally." --actor agent:builder-1 --reason "status update"
evidence <EVIDENCE-ID>
run=<RUN-ID> ticket=APP-1 type=note
actor=agent:builder-1 created_at=<TIMESTAMP>
title=First pass

Health check route added locally.

$ tracker run evidence add <RUN-ID> --type test_result --title "Smoke test" --body "go test ./... passed" --actor agent:builder-1 --reason "record verification"
evidence <EVIDENCE-ID>
run=<RUN-ID> ticket=APP-1 type=test_result
actor=agent:builder-1 created_at=<TIMESTAMP>
title=Smoke test

go test ./... passed

$ tracker run handoff <RUN-ID> --next-actor agent:reviewer-1 --next-gate review --actor agent:builder-1 --reason "ready for review"
# Handoff <HANDOFF-ID>

- Ticket: APP-1
- Run: <RUN-ID>
- Actor: agent:builder-1

## Status

Demo implementation started

## Evidence

- <EVIDENCE-ID>
- <EVIDENCE-ID>

## Next

- Actor: agent:reviewer-1
- Gate: review

```
