# Disaster Recovery

The v1.7 backup and disaster recovery contract is frozen in [backup-disaster-recovery.md](backup-disaster-recovery.md).

This compatibility page exists because the original v1.7 kickoff named `docs/disaster-recovery.md` as required release documentation. Keep both pages in sync if the recovery model changes.
